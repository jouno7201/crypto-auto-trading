# 📊 코인 자동매매 시스템 기획서 (Node 기반)

---

## 1. 프로젝트 개요

### 목적

- 전략 기반 자동매매 시스템 구축
- 전략 성능을 백테스트 + 실시간 운용 + 리포트로 검증
- 운영자가 직관적으로 상태를 파악하고 제어

### 핵심 기능

1. 전략 세팅
2. 백테스팅
3. 자동 매매
4. 시각화 대시보드
5. 리포트 생성

---

## 2. 전체 시스템 구조

[Frontend (Dashboard)]
↓ (REST / WebSocket)
[Node Server]
├ 전략 엔진
├ 백테스트 엔진
├ 매매 실행기
├ 리스크 관리
├ 데이터 저장(JSON 파일)
↓
[거래소 API (Upbit / Binance)]

---

## 3. 핵심 기능 정의

### 3.1 전략 세팅 기능

- 전략 선택 (MA, RSI, 볼린저, 변동성 돌파)
- 파라미터 설정
- 리스크 설정
- 종목 선택

### 3.2 백테스팅 기능

- 기간 설정
- 데이터 단위 설정
- 성과 분석 (수익률, MDD, 승률 등)

### 3.3 자동 매매 기능

- 전략 ON/OFF
- 실시간 시세 반영
- 주문 실행
- 리스크 관리

### 3.4 시각화 대시보드

- 자산 / 손익 / 포지션
- 캔들 차트
- 주문 및 로그

### 3.5 리포트 기능

- 성과 분석
- PDF / CSV 다운로드

---

## 4. 기술 스택

- Backend: Node.js, Express, WebSocket
- Frontend: HTML + JS
- 데이터 저장: JSON 파일 (Phase 3 이후 필요 시 SQLite → PostgreSQL 전환)

---

## 5. 데이터 모델 (JSON 파일 기반)

- `data/strategies.json` — 전략 설정 및 상태
- `data/backtest-results/` — 백테스트 결과 (날짜별 JSON)
- `data/trades.json` — 매매 기록
- `data/orders.json` — 주문 로그
- `data/snapshots/` — 자산 스냅샷 (날짜별 JSON)

---

## 6. 개발 단계 (상세 전략)

### Phase 0: 프로젝트 초기 세팅 (1주)

**목표:** 개발 환경 구축 및 프로젝트 뼈대 완성

| 항목                    | 내용                                                                              |
| ----------------------- | --------------------------------------------------------------------------------- |
| Node.js 프로젝트 초기화 | `package.json`, ESLint, Prettier, TypeScript 설정                                 |
| 폴더 구조 정의          | `src/` 하위에 `strategies/`, `engine/`, `api/`, `store/`, `routes/`, `dashboard/` |
| JSON 데이터 저장소 세팅 | `data/` 폴더 + JSON 읽기/쓰기 유틸 모듈 작성                                      |
| 거래소 API 연동 준비    | Upbit / Binance API 키 관리 (환경변수), SDK 래퍼 모듈 작성                        |
| Git 저장소 세팅         | `.gitignore`, 브랜치 전략(main/dev/feature), 커밋 컨벤션                          |

**산출물:** 빌드·실행 가능한 빈 프로젝트, JSON 데이터 저장소 유틸

---

### Phase 1: 데이터 수집 & 전략 엔진 MVP (2주)

**목표:** 시세 데이터 수집 + 기본 전략 로직 동작 확인

#### Step 1-1: 시세 데이터 파이프라인

- 거래소 REST API로 과거 캔들 데이터 수집 (OHLCV)
- WebSocket으로 실시간 시세 스트리밍
- 수집 데이터 JSON 파일 저장 및 메모리 캐싱

#### Step 1-2: 전략 엔진 구현

- 전략 인터페이스 정의 (`IStrategy`: `analyze()`, `signal()`)
- 기본 전략 구현:
  - 이동평균 크로스 (MA Cross)
  - RSI 과매수/과매도
  - 볼린저 밴드 이탈
  - 변동성 돌파 (래리 윌리엄스)
- 전략 파라미터 동적 설정 지원 (JSON Config)
- 전략별 유닛 테스트 작성

#### Step 1-3: 백테스트 엔진

- 과거 데이터 기반 시뮬레이션 루프 구현
- 성과 지표 계산: 총 수익률, MDD, 승률, 샤프 비율, 평균 보유 기간
- 슬리피지·수수료 모의 적용
- 결과를 `data/backtest-results/` JSON 파일에 저장

**산출물:** CLI에서 전략 선택 → 백테스트 실행 → 결과 출력 가능

---

### Phase 2: 자동 매매 실행기 (2주)

**목표:** 실제 주문 연동 및 리스크 관리 적용

#### Step 2-1: 주문 실행 모듈

- 거래소 주문 API 래핑 (시장가/지정가 매수·매도)
- 주문 상태 추적 (체결 대기 → 부분 체결 → 완전 체결 / 취소)
- 주문 실패 시 재시도 로직 (최대 3회, 지수 백오프)
- 모든 주문은 `data/orders.json`에 기록

#### Step 2-2: 리스크 관리 모듈

- 최대 투자 비중 제한 (종목당 / 전체)
- 손절(Stop-Loss) / 익절(Take-Profit) 자동 설정
- 일일 최대 손실 한도 초과 시 매매 중지
- 포지션 사이즈 계산 (켈리 공식 또는 고정 비율)

#### Step 2-3: 매매 스케줄러

- 전략 엔진 → 시그널 발생 → 주문 실행 파이프라인 연결
- Cron 또는 이벤트 기반 루프 (실시간 / 분봉 / 시봉 선택 가능)
- 전략별 ON/OFF 스위치 (JSON 설정 파일)
- 페이퍼 트레이딩 모드 (실제 주문 없이 시뮬레이션)

**산출물:** 페이퍼 트레이딩으로 전략 자동 실행 검증 완료

---

### Phase 3: 대시보드 & 시각화 (2주)

**목표:** 운영자가 시스템 상태를 실시간 모니터링하고 제어

#### Step 3-1: Express API 서버

- RESTful 엔드포인트 설계:
  - `GET /api/strategies` — 전략 목록/상태
  - `POST /api/strategies/:id/toggle` — 전략 ON/OFF
  - `GET /api/trades` — 매매 내역
  - `GET /api/assets` — 자산 현황
  - `GET /api/backtest` — 백테스트 실행/결과
- WebSocket 서버: 실시간 시세, 포지션 변동 푸시

#### Step 3-2: 프론트엔드 대시보드

- 대시보드 레이아웃 (HTML + Vanilla JS 또는 경량 프레임워크)
- 위젯 구성:
  - 자산 총액 / 일간 PnL 카드
  - 포지션 현황 테이블
  - 캔들 차트 + 매매 시점 마커 (lightweight-charts 활용)
  - 전략별 상태 카드 (ON/OFF 토글)
  - 최근 주문 로그 실시간 피드
- 반응형 레이아웃 (모바일 접근 가능)

**산출물:** 브라우저에서 실시간 모니터링 + 전략 제어 가능

---

### Phase 4: 리포트 & 알림 (1주)

**목표:** 성과 분석 자동화 및 운영 알림

#### Step 4-1: 리포트 생성

- 일간/주간/월간 성과 리포트 자동 생성
- 지표: 수익률, MDD, 승률, 거래 횟수, 수수료 총액
- PDF 리포트 생성 (puppeteer 또는 pdfkit)
- CSV 데이터 다운로드

#### Step 4-2: 알림 시스템

- 텔레그램 봇 연동 (주문 체결, 손절 발동, 시스템 에러)
- 알림 조건 설정 (수익률 임계치, 에러 발생 등)
- 알림 히스토리 저장

**산출물:** 자동 리포트 생성 + 텔레그램 실시간 알림

---

### Phase 5: 실거래 전환 & 안정화 (2주)

**목표:** 실제 자금 투입 전 최종 검증 및 안전장치 강화

#### Step 5-1: 실거래 준비

- 페이퍼 → 실거래 전환 플래그 (환경변수로 분리)
- 소액 테스트 (최소 금액으로 실주문 검증)
- 거래소 API Rate Limit 관리 (요청 큐 + 쓰로틀링)
- 네트워크 장애 대응 (연결 끊김 시 포지션 보호 로직)

#### Step 5-2: 보안 강화

- API 키 암호화 저장 (dotenv + 환경변수, 절대 코드에 하드코딩 금지)
- 대시보드 접근 인증 (JWT 기반 로그인)
- IP 화이트리스트 (거래소 API, 대시보드 모두)
- HTTPS 적용

#### Step 5-3: 모니터링 & 로깅

- 구조화된 로그 시스템 (winston/pino)
- 에러 추적 및 자동 복구 (프로세스 매니저: PM2)
- 시스템 헬스체크 엔드포인트 (`GET /health`)
- 데이터 파일 백업 자동화 (일 1회)

**산출물:** 실거래 안전하게 운영 가능한 프로덕션 환경

---

### Phase 6: 확장 & 고도화 (지속)

**목표:** 시스템 경쟁력 강화 및 확장성 확보

| 항목                 | 내용                                                          |
| -------------------- | ------------------------------------------------------------- |
| AI 전략 추가         | LSTM / 강화학습 기반 시그널 생성 (Python 마이크로서비스 연동) |
| 멀티 거래소          | Binance, Upbit 동시 운영, 거래소 간 차익거래 전략             |
| 멀티 전략 포트폴리오 | 전략 조합 최적화, 전략 간 자금 배분 (리밸런싱)                |
| 사용자 시스템 (SaaS) | 멀티 테넌트, 구독 모델, 사용자별 전략 관리                    |
| 인프라 고도화        | Docker 컨테이너화, CI/CD 파이프라인, 클라우드 배포 (AWS/GCP)  |

---

## 7. 리스크

- API 제한
- 슬리피지
- 보안

---

## 8. KPI

- 다운타임 0%
- 주문 실패율 <1%

---

## 9. 확장

- AI 전략
- SaaS화
